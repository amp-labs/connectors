package salesforce

import (
	"github.com/amp-labs/connectors/common"
)

func Read(spec common.ReadConfig) (common.Result, error) {
	// TODO: implement
	// Construct a SOQL query out of the spec, make the appropriate API calls, handle pagination, and return the full list of objects.
	return common.Result{}, nil
}
