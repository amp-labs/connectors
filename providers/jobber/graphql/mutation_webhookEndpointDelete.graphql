mutation WebhookEndpointDelete($ids: [EncodedId!]!) {
    webhookEndpointDelete(webhookEndpointsIds: $ids) {
        deletedWebhookEndpoints {
            id
            topic
            url
        }
        userErrors {
            message
            path
        }
    }
}
