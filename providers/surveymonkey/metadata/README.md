# Metadata

The static file `schemas.json` was created by manually inspecting the [SurveyMonkey API documentation](https://api.surveymonkey.com/v3/docs) and translating supported flat list endpoints into our static schema format.

Supported read objects include `surveys`, `contact_fields`, and `workgroups` in addition to the original catalog and contact objects.
