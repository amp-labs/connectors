query DisputeSearch {
  search {
    disputes(
      input: {
        {{if or .FromDate .ToDate}}
        createdAt: {
          {{if .FromDate}}
          greaterThanOrEqualTo: "{{.FromDate}}"
          {{end}}
          {{if .ToDate}}
          lessThanOrEqualTo: "{{.ToDate}}"
          {{end}}
        }
        {{end}}
      }
      first: {{.First}}
      {{if .After}}
      after: "{{.After}}"
      {{end}}
    ) {
      edges {
        node {
          id
          legacyId
          createdAt
          amountDisputed {
            value
            currencyCode
          }
          status
          type
          caseNumber
          referenceNumber
        }
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}

