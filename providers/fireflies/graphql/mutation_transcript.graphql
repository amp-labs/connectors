mutation {
    deleteTranscript(id:"{{.record_id}}") {
        id
        title
        host_email
        organizer_email
        fireflies_users
        participants
        date
        transcript_url
        audio_url
        duration
	}
}