# Metadata

The static file `schemas.json` was created by manually inspecting the [SendGrid API documentation](https://www.twilio.com/docs/sendgrid/api-reference) and [OpenAPI specs](https://github.com/twilio/sendgrid-oai), then translating the supported objects into our static schema format.
