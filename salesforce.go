package connectors

func salesforceRead(config ReadConfig) (Result, error) {
	// TODO: implement
	// Construct a SOQL query out of the config, make the appropriate API calls, handle pagination, and return the full list of objects.
	return Result{}, nil
}
